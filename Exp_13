#include <stdio.h>

int climbStairs(int n)
{
    int a=1,b=1,temp;

    for(int i=0;i<n;i++)
    {
        temp=a;
        a=b;
        b=temp+b;
    }

    return a;
}

int main()
{
    printf("%d\n",climbStairs(4));
    printf("%d\n",climbStairs(3));
}
